import { Doc } from "../convex/_generated/dataModel";

// Define the type for an alert object, matching the schema
export type PriceAlertsTableName = "priceAlerts";
export type PriceAlertType = Doc<PriceAlertsTableName>;

// This file can be used for other shared application types.
